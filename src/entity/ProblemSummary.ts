
export interface IProblemSummary {

    PROGRAMMING?: {
        total: number;
        totalScore: number;
    }

    CODE_COMPLETION?: {
        total: number;
        totalScore: number;
    }

    MULTIPLE_FILE?: {
        total: number;
        totalScore: number;
    }
}
